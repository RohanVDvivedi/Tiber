* PICKED TASKS

* TIBER

  * implement tiber_epoll
    * that has an infinite loop function that does IO
    * maintains a data struct for 
      struct tiber_epoll_data
      {
        // file descriptor
        int fd;

        tiber_mutex lock;

        // for read side
        singlylist read_waiters;

        // for write side
        singlylist write_waiters;

        bstnode fd_list;
      }
    * every waiter that gets EAGAIN/EWOULDBLOCK/EINPROGRESS waits by putting itself in the waiters queue using a struct
      struct tiber_epoll_waiter
      {
        tiber* tb;
        tiber_cond wait;

        slnode embed_node_for_read_waiters;
        slnode embed_node_for_write_waiters;
      };
    * for tiber_read(int fd, ...) or tiber_write(int fd, ...) or even tiber_connect and tiber_accept
      * in a loop first attempt to call it optimistically, if it fails with EAGAIN/EWOULDBLOCK/EINPROGRESS (always do the optimistic version first)
      * take lock
      * attempt to do non-blocking read or write again using the actual system call
      * if it fails with EAGAIN/EWOULDBLOCK/EINPROGRESS, then construct a local tiber_epoll_waiter and push it into the queue
      * then go into waiting on the wait
      * once woken up, remove your self from both the waiters queue and loop over again
    * epoll loop is simple waits for N events, once received, based on the event takes lock removed the reader or writer or both and signals them and quits
    * the epoll loop maintains the hashmap of 256 redblack tree based buckets, poiting fd to their tiber_epoll_data
    * registering file descriptors and closing it will be taken in account later

  * find a way to safely replace all the pthread downstreams with this
    * using library reordering and -include flag to #include a converted that #defines all pthread structs and functions to tiber structs and functions
  * find a way to integrate it into the main thread
    * make main thread create a global tiber_runtime with 24KB stack space on threads and thread count being the number of CPU cores
    * use default runtime to create new tibers